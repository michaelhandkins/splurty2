<!DOCTYPE html>
<html>
  <head>
    <title>Splurty2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!--- Navbar Begins --->
    <header class="row">
      <div class="col-3">
        <%= link_to root_path, class: 'logo' do %>
          <div class="circle">
            <br />
            YOLO
            <br />
            LOL
            <br />
            OMG
          </div>
        <% end %>
      </div>
      <div class="col col-sm-4 slogan hidden-xs-down">
        Splurting out unsolicited advice since 2019.
      </div>
      <div class="col-10 offset-1 offset-sm-0 col-sm-5 links">
        <%= link_to "About", about_path %>
        &nbsp;&nbsp;
        <%= link_to "Contribute", "#", data: {toggle: 'modal', target: '#myModal'} %>
      </div>

    </header>


    <!-- Modal -->
    <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">×</span>
            </button>
            <h4 class="modal-title" id="myModalLabel">Contribute Your Quote</h4>
          </div>
          <%= simple_form_for Quote.new do |f| %>
            <div class="modal-body">
              <h1>Enter Your Quote Details</h1>
                <%= f.input :saying %>
                <%= f.input :author %>
                <br />
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
              <%= f.submit "Create", class: 'btn btn-danger' %>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <hr />
    <br class="clear" />
    <!--- Navbar Ends --->

    <!--- Individual Page Content --->
    <div class="container">
      <% if flash[:error].present? %>
        <div class="col-10 offset-1 alert alert-danger">
          <%= flash[:error].html_safe %>
        </div>
      <% end %>
      <%= yield %>
    </div>
    <!--- Page Content Ends --->

    <!-- Sticky Footer Begins --->
    <div class="footer">
      <div class="container">
        <div class="row">
          <div class="col-sm-6 col-12">
            <%= link_to "This isn't enough, I need more.", root_path, class: 'next-quote' %>
          </div>
          <div class="col-sm-6 col-12 footer-blurb">
            Proudly hacked by <a href="https://michaelsportfolio.netlify.com/">Michael Handkins</a>
          </div>
        </div>
      </div>
    </div>
    <!--- Sticky Footer Ends --->

  </body>
</html>
